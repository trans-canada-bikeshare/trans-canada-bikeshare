# Spec 025 — Methodology and data quality

## Status

Complete. Shipped 2026-07-29.

> **Written retrospectively on 2026-07-29**, from `3edb79a and neighbours`. This records what shipped; it did not guide it. The original overnight run built these features without writing their specs first, and inventing a plan after the fact would misrepresent how the work happened.

## What shipped

The `#method` section: what the pipeline dropped, what it flagged, and what
rests on a weaker claim. Figures come from `exclusions.json`, generated by the
same run that produces every other artifact, so the section cannot drift from
the data it describes.

Sources, licences and limitations are stated plainly — including that **BIXI
publishes no licence at all**, which is recorded in the manifest, in `LICENSE`,
and on the site rather than softened or omitted.

## Where the record is

- `src/App.tsx` — `#method`
- `docs/data-quality-report.md` — the full generated accounting
- `docs/decisions.md`, 2026-07-28 entry on the BIXI licence
